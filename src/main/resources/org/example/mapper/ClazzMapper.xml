<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.ClazzMapper">
    <select id="list" resultType="org.example.pojo.Clazz">
        select c.* ,e.name masterName,
               (case
                   when c.begin_date>curdate() then '未开班'
                   when curdate() between c.begin_date and c.end_date then '在读'
                   else '已结课'
               end) status
        from clazz c
        left join emp e on c.master_id = e.id
        <where>
            <if test="name != null and name!=''">
                and c.name like CONCAT('%', #{name}, '%')
            </if>
            <if test="begin != null and end != null">
                and c.begin_date between #{begin} and #{end}
            </if>
        </where>
        order by c.update_time desc
    </select>
<!--    <select id="findById" resultType="org.example.pojo.Clazz">-->
<!--        select * from clazz where id = #{id}-->
<!--    </select>-->
</mapper>